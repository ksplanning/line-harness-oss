case-scope-echo: caseId=keyword-unresponded-scope target_paths=["/root/.openclaw/line-harness-ks/.plans/2026-07-22-keyword-unresponded-scope/tasks.md"]

root-cause: `apps/worker/src/services/unanswered-inbox.ts:106-129` は `last_incoming > last_manual` で未対応候補を作り、機械送信の auto_reply を manual に数えない。従来の成功 marker は未読と未対応を同時に除外するため、ルール単位で「未読0・未対応1」を表現できなかった。`apps/worker/src/routes/webhook.ts:608-615,763-800` で送信成功後だけ受信時の専用 marker を確定し、`apps/worker/src/routes/conversations.ts:11-12` は未読から除外、`apps/worker/src/services/unanswered-inbox.ts:313-320` は未対応へ残すよう勘定を分離した。失敗・停止時は `apps/worker/src/routes/webhook.ts:823-825` の従来経路へ残る。

TDD Red（実装前）: worker は専用 marker が `auto_reply_keyword` のまま、opt-in 未対応件数が期待1に対して0、未読 SQL に専用 marker がなく失敗。DB は migration 125 不在と CRUD 値欠落、Web はチェックボックスと一覧表示不在で失敗。その後に最小実装を入れて下記 Green を確認した。

- D-1: PASS — `pnpm --filter worker exec vitest run src/routes/webhook.test.ts src/routes/conversations.keyword-unread.test.ts src/services/unanswered-inbox.test.ts src/services/auto-reply-keyword-match.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000` → 4 files / 77 tests passed（成功既定=未読0・未対応0、keep=1=未読0・未対応1、失敗・OFF=未読1・未対応1、通常=未読1・未対応1） (exit 0)
- D-2: PASS — `pnpm --filter @line-crm/db exec vitest run src/auto-replies.test.ts test/125_auto_reply_keep_in_unresponded.test.ts test/bootstrap.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter worker exec vitest run src/routes/auto-replies.multi-message.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter web exec vitest run src/app/auto-replies/auto-replies-embed.test.tsx src/components/auto-replies/edit-dialog.test.tsx --reporter=dot --maxWorkers=1 --testTimeout=30000` → DB 3 files / 11 tests、Worker API 1 file / 12 tests、Web 2 files / 19 tests passed（migration 125 default 0、bootstrap、CRUD/API、チェックボックス説明・保存復元・一覧表示） (exit 0)
- D-3: PASS — `pnpm --filter @line-crm/db exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter worker exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter web exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-crm/shared exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-crm/update-engine exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-harness/sdk exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm test:scripts -- --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm -r --workspace-concurrency=2 --if-present run typecheck && pnpm --filter web exec tsc --noEmit --incremental false --pretty false` → 全653 files / 6,254 tests passed、全対象typecheck passed、base 460e34a0f 比で保護4ファイルと既存migration 109件は不変・追加は125のみ (exit 0)
- D-4: PASS — `rg -n -e '# keyword-unresponded-scope' -e '既定ルール' -e '未対応リストに残す' -e '通常メッセージ' -e 'オーナー向けの日常語' .sola/live-checklist.md` → host実射4分岐、過去分不変、安全境界、owner日常語、実測記入欄を検出 (exit 0)

note: D-4 の PASS は host closer 用手順書の整備・機械確認を示す。sandbox から deploy、migration、本番 DB 操作、LINE 実射は実施していない。

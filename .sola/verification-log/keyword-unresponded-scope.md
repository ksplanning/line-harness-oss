case-scope-echo: caseId=keyword-unresponded-scope target_paths=["/root/.openclaw/line-harness-ks/.plans/2026-07-22-keyword-unresponded-scope/tasks.md"]

root-cause: account scope の非対称経路をコードで確定した。`apps/worker/src/routes/webhook.ts:127-139` は env secret の署名検証に成功しても対応する active `line_accounts` 行が無ければ `matchedAccountId=null` のままになり、`apps/worker/src/routes/webhook.ts:557-580` は global auto_reply だけを照合する。ところが修正前の `apps/worker/src/services/event-bus.ts` は `!lineAccountId` で全 account-scoped automation を許可していたため、scoped exception rule は `matchedRule` にならない一方で別 account 用 automation reply は送信できた。`apps/worker/src/routes/webhook.ts:612-616,848-863` の成功 marker 確定は `matchedRule` 必須なので incoming は `user_unmatched` のまま、`apps/worker/src/services/unanswered-inbox.ts:102-129,155-173` は `last_incoming > last_manual` かつ `automation` outgoing を自動返信証拠に数えないため、「返信は届いたが未対応」が成立した。Piecemaker の inline 実測は account/rule が解決され incoming が `source=auto_reply_keyword` まで確定した経路なので未読+0・未対応+0となり、この差と矛盾しない。

fix-scope: `apps/worker/src/services/event-bus.ts:51-58,156-162` は account を tri-state 化し、string は同一 account + global、webhook が明示する `null` は global のみ、account context を持たない旧 caller の `undefined` は従来どおり全 scope とした。これで account 未解決時の auto_reply と automation の scope が一致し、別 account 用 token 誤送信も止まる。新規 text incoming は `apps/worker/src/routes/webhook.ts:620-631` で fail-closed marker から始め、返信するルールは `apps/worker/src/routes/webhook.ts:764-804` で外部送信成功後だけ marker を確定する。`silent` は `apps/worker/src/routes/webhook.ts:612-616,715-717` で「返信しない」こと自体を正常処理とし、`apps/worker/src/routes/webhook.ts:835-858` で event-bus dispatch 後、automation の有無・送信成否に依存せず rule 固有 marker を確定する。marker 保存失敗だけは provisional のまま未読へ戻す。`apps/worker/src/routes/conversations.ts:11-12` は既定 marker と opt-in marker の双方を未読から除外し、`apps/worker/src/services/unanswered-inbox.ts:313-324` は opt-in marker だけ未対応へ残す。返信するルールの送信失敗、ルールOFF、account不明の scoped rule、通常メッセージは未読+未対応へ残る。新規行の default/opt-in はキーワード本文や後日のルール編集ではなく、着信時に match した rule の保存 marker だけで決まる。postback は従来どおり集計対象外で、本件は rich menu の `action.type=message` が作る利用者発言だけを対象とする。

r2-root-cause: 査読済み HEAD `06065fa7c` では commit `82142320b` が、従来は rule match だけで完了していた `silent` を `automationMessageSent=true` 待ちへ変更していた。`apps/worker/src/services/event-bus.ts:154-198` は送信 automation 無しと送信 dead-letter の双方で false を返すため、旧 `apps/worker/src/routes/webhook.ts:848-862` は marker を `user_unmatched` のまま残して `upsertChatOnMessage` を呼び、未読1・未対応1へ退行した。追加 commit `e3640f85c` は optional automation の結果依存だけを除き、返信型ルールの送信成功条件・schedule 抑止・marker 保存失敗の fail-closed は変えていない。

TDD Red（実装前）: worker は専用 marker が `auto_reply_keyword` のまま、opt-in 未対応件数が期待1に対して0、未読 SQL に専用 marker がなく失敗。DB は migration 125 不在と CRUD 値欠落、Web はチェックボックスと一覧表示不在で失敗。独立レビュー後の追加 Red では webhook/event-bus が 8 failed / 40 passed（silent marker の早期確定、automation dead-letter が未読にならない、away/cross-account opt-in 欠落、event-bus の送信結果欠落）、SDK fixture は新 field の create/update/list 型4箇所で TS2353/TS2339 になった。最終再査読後の account-null Red は 2 files で 1 failed / 50 passed、`automationMessageSent` が期待 false に対して実際 true となり scoped automation の漏れを再現した。R2 は実装前に `silent` + automation 無しと dead-letter の fixture を反転し、webhook 1 file で 2 failed / 38 passed（どちらも source update 不在）を観察した。最小修正後は同 file 40/40、関連5 files 95/95へ Green 化した。

- D-1: PASS — `pnpm --filter worker exec vitest run src/routes/webhook.test.ts src/services/event-bus.test.ts src/routes/conversations.keyword-unread.test.ts src/services/unanswered-inbox.test.ts src/services/auto-reply-keyword-match.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000` → 5 files / 95 tests passed（返信成功既定=未読0・未対応0、keep=1=未読0・未対応1、silent+automation無し=未読0・未対応0、silent automation dead-letter+keep=1=未読0・未対応1、返信型送信失敗・ルールOFF・account不明scoped rule・通常メッセージ=未読1・未対応1、旧unscoped caller互換） (exit 0)
- D-2: PASS — `pnpm --filter @line-crm/db exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000` / `pnpm --filter worker exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000` / `pnpm --filter web exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000` / `pnpm --filter @line-harness/sdk exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000` / `pnpm --filter @line-harness/sdk exec tsc --noEmit --target ES2022 --module ESNext --moduleResolution bundler --strict --skipLibCheck --lib ES2022,DOM tests/resources/auto-replies.test.ts` / `pnpm exec tsx scripts/check-migrations.ts packages/db/migrations/125_auto_reply_keep_in_unresponded.sql` / `pnpm --dir packages/db generate:bootstrap --check` → DB 90 files / 604 tests、Worker 276 / 3,282、Web 212 / 1,612、SDK 12 / 53、public型 fixture、migration checker 1件、bootstrap同期が通過（additive migration 125・既存行default 0、CRUD/API/SDK、チェックボックス説明・保存復元・一覧表示） (exit 0)
- D-3: PASS — `pnpm --filter @line-crm/db exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter worker exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter web exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-crm/shared exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-harness/update-engine exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-harness/sdk exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm test:scripts -- --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm -r --workspace-concurrency=2 --if-present run typecheck && pnpm --filter web exec tsc --noEmit --incremental false --pretty false` / `git diff --exit-code 460e34a0f -- apps/worker/src/routes/formaloo-public.ts apps/worker/src/services/formaloo-webhook.ts apps/worker/src/services/formaloo-row-edit.ts apps/worker/src/services/formaloo-friend-token.ts && git diff --diff-filter=MDR --exit-code 460e34a0f -- packages/db/migrations` → 全654 files / 6,265 tests passed（Worker 276 / 3,282、実名 `@line-harness/update-engine` 14 / 117）、11 workspace projects + Web追加typecheck passed、base 460e34a0f 比で保護4ファイルと既存migration 109件は不変・追加は125のみ。返信型失敗・account-scope安全条件と、silent無送信の既契約を明示 fixture で確認 (exit 0)
- D-4: PASS — `rg -n -e '# keyword-unresponded-scope' -e 'Piecemaker と KS' -e 'matchedAccountId' -e 'action.type=message' -e '既定ルール' -e '未対応リストに残す' -e '送信を伴う automation が無い' -e '通常メッセージ' -e '実測記入欄' -e 'オーナー向けの日常語' .sola/live-checklist.md` → KS実経路とPiecemaker既知結果のaccount解決比較、host実射4分岐、silent+送信automation無し、postback境界、過去分不変、安全境界、owner日常語、実測記入欄を検出 (exit 0)

note: D-4 の PASS は host closer 用手順書の整備・機械確認を示す。sandbox から deploy、migration、本番 DB 操作、LINE 実射は実施していない。

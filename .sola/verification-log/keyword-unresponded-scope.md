case-scope-echo: caseId=keyword-unresponded-scope target_paths=["/root/.openclaw/line-harness-ks/.plans/2026-07-22-keyword-unresponded-scope/tasks.md"]

root-cause: account scope の非対称経路をコードで確定した。`apps/worker/src/routes/webhook.ts:127-139` は env secret の署名検証に成功しても対応する active `line_accounts` 行が無ければ `matchedAccountId=null` のままになり、`apps/worker/src/routes/webhook.ts:557-580` は global auto_reply だけを照合する。ところが修正前の `apps/worker/src/services/event-bus.ts` は `!lineAccountId` で全 account-scoped automation を許可していたため、scoped exception rule は `matchedRule` にならない一方で別 account 用 automation reply は送信できた。`apps/worker/src/routes/webhook.ts:612-616,848-863` の成功 marker 確定は `matchedRule` 必須なので incoming は `user_unmatched` のまま、`apps/worker/src/services/unanswered-inbox.ts:102-129,155-173` は `last_incoming > last_manual` かつ `automation` outgoing を自動返信証拠に数えないため、「返信は届いたが未対応」が成立した。Piecemaker の inline 実測は account/rule が解決され incoming が `source=auto_reply_keyword` まで確定した経路なので未読+0・未対応+0となり、この差と矛盾しない。

fix-scope: `apps/worker/src/services/event-bus.ts:51-58,156-162` は account を tri-state 化し、string は同一 account + global、webhook が明示する `null` は global のみ、account context を持たない旧 caller の `undefined` は従来どおり全 scope とした。これで account 未解決時の auto_reply と automation の scope が一致し、別 account 用 token 誤送信も止まる。新規 text incoming は `apps/worker/src/routes/webhook.ts:620-631` で fail-closed marker から始め、inline は `apps/worker/src/routes/webhook.ts:763-803`、silent + automation は `apps/worker/src/routes/webhook.ts:830-863` と `apps/worker/src/services/event-bus.ts:145-198` で外部送信完了後だけ rule 固有 marker へ確定する。`apps/worker/src/routes/conversations.ts:11-12` は既定 marker と opt-in marker の双方を未読から除外し、`apps/worker/src/services/unanswered-inbox.ts:313-324` は opt-in marker だけ未対応へ残す。送信失敗、dead-letter、ルールOFF、account不明のscoped rule、marker保存失敗は provisional のまま未読+未対応へ残る。新規行の default/opt-in はキーワード本文や後日のルール編集ではなく、着信時に match した rule の保存 marker だけで決まる。postback は従来どおり集計対象外で、本件は rich menu の `action.type=message` が作る利用者発言だけを対象とする。

TDD Red（実装前）: worker は専用 marker が `auto_reply_keyword` のまま、opt-in 未対応件数が期待1に対して0、未読 SQL に専用 marker がなく失敗。DB は migration 125 不在と CRUD 値欠落、Web はチェックボックスと一覧表示不在で失敗。独立レビュー後の追加 Red では webhook/event-bus が 8 failed / 40 passed（silent marker の早期確定、automation dead-letter が未読にならない、away/cross-account opt-in 欠落、event-bus の送信結果欠落）、SDK fixture は新 field の create/update/list 型4箇所で TS2353/TS2339 になった。最終再査読後の account-null Red は 2 files で 1 failed / 50 passed、`automationMessageSent` が期待 false に対して実際 true となり scoped automation の漏れを再現した。その後に最小実装を入れて下記 Green を確認した。

- D-1: PASS — `pnpm --filter worker exec vitest run src/routes/webhook.test.ts src/services/event-bus.test.ts src/routes/conversations.keyword-unread.test.ts src/services/unanswered-inbox.test.ts src/services/auto-reply-keyword-match.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000` → 5 files / 94 tests passed（成功既定=未読0・未対応0、keep=1=未読0・未対応1、inline/silent成功後だけ抑止、送信失敗・dead-letter・OFF・account不明scoped rule=未読1・未対応1、通常=未読1・未対応1、旧unscoped caller互換） (exit 0)
- D-2: PASS — `pnpm --filter @line-crm/db exec vitest run src/auto-replies.test.ts test/125_auto_reply_keep_in_unresponded.test.ts test/bootstrap.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter worker exec vitest run src/routes/auto-replies.multi-message.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter web exec vitest run src/app/auto-replies/auto-replies-embed.test.tsx src/components/auto-replies/edit-dialog.test.tsx --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-harness/sdk exec vitest run tests/resources/auto-replies.test.ts --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-harness/sdk exec tsc --noEmit --target ES2022 --module ESNext --moduleResolution bundler --strict --skipLibCheck --lib ES2022,DOM tests/resources/auto-replies.test.ts && pnpm exec tsx scripts/check-migrations.ts packages/db/migrations/125_auto_reply_keep_in_unresponded.sql && pnpm --dir packages/db generate:bootstrap --check` → DB 3 files / 11 tests、Worker API 1 file / 12 tests、Web 2 files / 19 tests、SDK 1 file / 2 tests + public型 fixture、migration checker、bootstrap同期が通過（additive migration 125・既存行default 0、CRUD/API/SDK、チェックボックス説明・保存復元・一覧表示） (exit 0)
- D-3: PASS — `pnpm --filter @line-crm/db exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter worker exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter web exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-crm/shared exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-crm/update-engine exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm --filter @line-harness/sdk exec vitest run --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm test:scripts -- --reporter=dot --maxWorkers=1 --testTimeout=30000 && pnpm -r --workspace-concurrency=2 --if-present run typecheck && pnpm --filter web exec tsc --noEmit --incremental false --pretty false && git diff --exit-code 460e34a0f -- apps/worker/src/routes/formaloo-public.ts apps/worker/src/services/formaloo-webhook.ts apps/worker/src/services/formaloo-row-edit.ts apps/worker/src/services/formaloo-friend-token.ts` → 全654 files / 6,264 tests passed（最終Worker 276 files / 3,281 tests）、11 workspace projects + Web追加typecheck passed、base 460e34a0f 比で保護4ファイルと既存migration 109件は不変・追加は125のみ、失敗時・account-scope安全条件の明示テストも通過 (exit 0)
- D-4: PASS — `rg -n -e '# keyword-unresponded-scope' -e 'Piecemaker と KS' -e 'matchedAccountId' -e 'action.type=message' -e '既定ルール' -e '未対応リストに残す' -e '通常メッセージ' -e '実測記入欄' -e 'オーナー向けの日常語' .sola/live-checklist.md` → KS実経路とPiecemaker既知結果のaccount解決比較、host実射4分岐、postback境界、過去分不変、安全境界、owner日常語、実測記入欄を検出 (exit 0)

note: D-4 の PASS は host closer 用手順書の整備・機械確認を示す。sandbox から deploy、migration、本番 DB 操作、LINE 実射は実施していない。

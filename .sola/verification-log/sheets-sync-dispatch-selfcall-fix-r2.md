case-scope-echo: caseId=sheets-sync-dispatch-selfcall-fix-r2 target_paths=["/root/.openclaw/line-harness-ks/.plans/2026-07-22-sheets-sync-dispatch-selfcall-fix-r2/tasks.md"]

verification-base: f32418649
verification-code-head: 1d108548a
verification-mode: lane local implementation and self-verification complete; production deploy and processed>0 host evidence remain the closer-owned step defined by tasks.md

# sheets-sync-dispatch-selfcall-fix-r2 — verification

TDD Red evidence: `pnpm --filter worker exec vitest run src/services/sheets-sync-jobs.test.ts --maxWorkers=1 -t 'drains 1,450'` → `processSheetsSyncJobBatch is not a function` (exit 1 expected before implementation); `pnpm --filter worker exec vitest run src/services/sheets-sync-jobs.cron.test.ts --maxWorkers=1 -t 'advances cron work inline'` → expected inline batch call but calls were 0 while old self-dispatch failed (exit 1 expected before implementation).

Independent review: current code head `1d108548a` has no findings; residual risk is limited to host Cloudflare and real Google Sheets duration/subrequest behavior, covered by the closer checklist.

- D-1: PASS — `pnpm --filter worker exec vitest run src/services/sheets-sync-jobs.test.ts src/services/sheets-sync-jobs.cron.test.ts src/routes/sheets-connections.inline-sync.test.ts src/services/friend-ledger-sync.cron.test.ts src/routes/sheets-friend-ledger.test.ts --maxWorkers=1` and `if rg -n 'dispatchSheetsSyncWork' apps/worker/src/services/sheets-sync-dispatch.ts apps/worker/src/routes/sheets-connections.ts apps/worker/src/index.ts; then exit 1; fi` → 5 files / 40 tests passed; permanent safe logs expose `self_binding`, receiving `origin`, verification result, trigger, chunk and processed count; same-origin HMAC is accepted, rewritten-origin HMAC is rejected with 401, caller env is retained, and Sheets self-dispatch references are 0 (exit 0)
- D-2: PASS — `pnpm --filter worker exec vitest run src/services/sheets-sync-jobs.test.ts src/services/sheets-sync-jobs.cron.test.ts src/routes/sheets-connections.inline-sync.test.ts src/services/friend-ledger-sync.cron.test.ts src/routes/sheets-friend-ledger.test.ts --maxWorkers=1` → manual runner completes 1,450 rows as `200→400→600→800→1000→1200→1400→1450`, an over-limit 1,800-row job stops durably at 1,600 after eight chunks and the next cron poll completes 1,800, manual/cron call no Sheets SELF or public self-fetch, and origin-bound HMAC rejection remains intact; 5 files / 40 tests passed (exit 0)
- D-3: PASS — `pnpm --filter worker exec vitest run --maxWorkers=2`; `pnpm --filter web exec vitest run --config vitest.config.ts --maxWorkers=1`; `pnpm --filter @line-crm/db test`; `pnpm --filter @line-crm/shared test`; worker/web/db/shared typechecks; DB bootstrap check; rich-menu cron focused test; generic dispatcher/config and migration diff checks → Worker 282 files / 3,373 tests, Web 220 / 1,666, DB 93 / 626, Shared 42 / 532, all four TypeScript checks, DB bootstrap, rich-menu 15 tests passed; `internal-work-dispatch.ts` and both SELF configs unchanged; migration changes 0 (exit 0)
- D-4: PASS — `git ls-files --error-unmatch .sola/live-checklist.md` and round2-section `rg` checks for manual/cron `processed=0→200→1450`, evidence paths, second-run duplicate 0, disposable sheet, fictional friends, production-sheet/three-form isolation, LINE sends 0 and cleanup → tracked host checklist covers separate manual and real five-minute cron jobs, processed>0 and completion screenshot/safe-log evidence slots, 200-row progress, 1,450-row completion, duplicate 0 and safety boundaries; live execution is explicitly reserved for host closer (exit 0)

node_modules/
dist/
.next/
.wrangler/
.vercel/
.env
.env.local
*.log
.DS_Store
.mcp.json
*.toml.bak
.env.production
.env.staging
# wrangler local secrets (FORMALOO_* 等の生値・PUBLIC repo に絶対 commit しない / D-2 / R1 F3)
.dev.vars
**/.dev.vars
apps/web/out/
apps/worker/dist/
.worktrees/
.claude/worktrees/

# --- backups / temp ---
*.bak
*.tmp
*~
# harness runtime state (never commit to this PUBLIC repo — may contain D1 dumps with secrets)
.ars-state/
.ars-state/backups/
